services:
  - type: web
    name: final-project-airon-chefs-backend
    env: node
    plan: free
    buildCommand: "npm run build"
    startCommand: "npm start"
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
