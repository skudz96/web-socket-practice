services:
  - type: web
    name: final-project-airon-chefs-backend
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server/server.js"
    envVars:
      - key: NODE_ENV
        value: production
